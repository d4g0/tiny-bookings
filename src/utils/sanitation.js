export function sanitaizeCreateAdminInput({
    // creator_admin_id, createAdminService will throw if non valid
    // user_role, createAdminService will throw if non valid
    // password, createAdminService will throw if non valid
    email,
    admin_name,
    admin_description,
}) {

}